name: Test DF

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 18 * * 0'

jobs:
  test-df-output:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Rust cache
        uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.toml') }}
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with: {profile: "minimal", toolchain: "stable"}
      - name: Install Python
        uses: actions/setup-python@v2
      - name: Load cached Poetry installation
        if: runner.os != 'Windows'
        uses: actions/cache@v2
        with:
          path: ~/.local
          key: poetry-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('.github/workflows/test_df.yml') }}
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true
      - name: Python cache
        uses: actions/cache@v2
        with:
          path: |
            .venv
            ~/.cache/pip/
          key: venv-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('DeepFilterNet/pyproject.toml') }}
      - name: Install build dependencies
        run: pip install maturin
      - name: Install DeepFilterNet
        working-directory: DeepFilterNet
        run: poetry install -E eval --no-interaction
      - name: Run Test
        run: |
          source $VENV
          # python DeepFilterNet/df/scripts/test_df.py
