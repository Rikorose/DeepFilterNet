[workspace]
resolver = "2"

members = [
  "libDF",
  "pyDF",
  "pyDF-data"
]

[profile.dev]
opt-level = 1
incremental = false
lto = false

[profile.release]
opt-level = 3
lto = "thin"

[profile.release-lto]
inherits = "release"
lto = "fat"
strip = true
